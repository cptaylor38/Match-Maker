$.get('/api/friends') //displays json of all possible friends

$.post('/api/friends', function () {
    for (var i = 0; i < something.length; i++) {

    }
});


//handle incoming survey results and compatability logic